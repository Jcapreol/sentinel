ANTHROPIC_API_KEY=
VIRUSTOTAL_API_KEY=
ABUSEIPDB_API_KEY=
URLHAUS_API_KEY=
# SENTINEL_TIMEOUT=10
# EVAL_CORPUS_PATH=/path/to/eval-corpus
# Encrypts every persisted triage evidence record (sentinel-triage only).
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Never commit a real value here or anywhere else -- see docs/security.md
# for backup/rotation guidance before setting this.
# SENTINEL_EVIDENCE_KEY=
